# ตั้งโปรเจกต์ใหม่หลังลบ Supabase เดิม (ผู้ใช้ชุดเดิม)

ใช้คู่มือนี้เมื่อ **ลบ Supabase project เดิมทิ้งแล้ว** และต้องการสร้างใหม่
โดย **ผู้ใช้ยังเป็นชุดเดิม** (ปิ่น / ต้น / บ้านเรา — คนละเคสกับ `SETUP-NEW.md` ที่ไว้ตั้งให้บ้านอื่น)

**สิ่งที่หายไปแล้วถาวร (กู้คืนไม่ได้):** ข้อมูลใน `txns`, ประวัติแก้ไขใน `txn_edits`,
ไฟล์สลิปทั้งหมดใน bucket `slips` — โปรเจกต์ใหม่เริ่มจากศูนย์ ต้องใส่ **ยอดยกมา** เอง

**สิ่งที่ยังใช้ของเดิมได้ ไม่ต้องแก้:** โค้ดทั้งหมด (`.dc.html`, `support.js`, `_ds/`),
ไฟล์ SQL ทุกไฟล์ (อีเมลในนั้นตรงกับชุดผู้ใช้เดิมอยู่แล้ว), GitHub repo/Pages เดิม

---

## ขั้นที่ 1 · สร้าง Supabase project ใหม่

1. **supabase.com → New project** ตั้งชื่ออะไรก็ได้ เลือก region ใกล้ๆ ตั้งรหัสผ่าน database
2. รอสร้างเสร็จ (~1-2 นาที)
3. **Project Settings → API** จดสองค่านี้ไว้:
   - **Project URL** — เช่น `https://xxxxxxxx.supabase.co`
   - **publishable key** — ขึ้นต้น `sb_publishable_...`

> ⚠️ **ห้ามใช้ `service_role` key เด็ดขาด** — ตัวนั้นข้าม RLS ได้ทั้งหมด
> ถ้าเอาไปใส่ใน `config.js` = เปิดฐานข้อมูลทิ้งไว้ให้ทุกคนบนอินเทอร์เน็ต

## ขั้นที่ 2 · สร้างผู้ใช้ 3 คนในโปรเจกต์ใหม่ (ย้าย user)

**Authentication → Users → Add user → Create new user** ทำให้ครบทั้ง 3 คน ด้วย **อีเมลเดิม**:

| อีเมล | ชื่อในสมุด | สิทธิ์ |
| --- | --- | --- |
| `jianengyang11@gmail.com` | ปิ่น | admin |
| `jia@gmail.com` | ต้น | member |
| `yang11@gmail.com` | บ้านเรา | member |

- ตั้งรหัสผ่านใหม่ให้แต่ละคน (จำไว้ ใช้ล็อกอินหน้าเว็บ)
- **ติ๊ก Auto Confirm User ทุกคน** (ไม่ติ๊ก = ล็อกอินไม่ได้เพราะรออีเมลยืนยัน)

แล้วไป **Authentication → Sign In / Providers** → ปิด **Allow new users to sign up**
(ไม่ปิด = ใครก็สมัครเข้ามาเองได้)

## ขั้นที่ 3 · รัน SQL ตามลำดับนี้เท่านั้น — ทีละไฟล์

**SQL Editor → New query → Ctrl+A เลือกทั้งไฟล์ → วาง → Run** (อย่ารันแค่บางส่วนที่ไฮไลต์)

ไฟล์ทั้งหมดอยู่ที่ `C:\JIA\สรุปลายรับลายจ่าย\` — **ไม่ต้องแก้เนื้อไฟล์เลย**
เพราะอีเมล → ชื่อบัญชีใน `schema.sql` บล็อก 5 ตรงกับตารางข้างบนอยู่แล้ว

| ลำดับ | ไฟล์ | ต้องได้ |
| --- | --- | --- |
| 1 | `schema.sql` | 3 แถว และปิ่นต้องเป็น `admin` |
| 2 | `fix_txn_edits.sql` | ตามที่ไฟล์บอก |
| 3 | `upgrade_v2.sql` | 1 แถว ทุกช่องเป็น `true` |
| 4 | `upgrade_v3.sql` | ✓ ทั้ง 2 แถว |
| 5 | `upgrade_v4.sql` | ✓ ทั้ง 4 แถว |

ถ้าไฟล์ 1 ไม่ขึ้นชื่อครบทั้ง 3 คน แปลว่าอีเมลที่สร้างในขั้นที่ 2 สะกดไม่ตรงกับ `schema.sql`
เช็คให้ตรงแล้วรันไฟล์ 1 ซ้ำได้ (รันซ้ำไม่พัง)

## ขั้นที่ 4 · แก้ `config.js` แล้วอัปขึ้น GitHub

เปิด `config.js` แก้แค่ 2 บรรทัดนี้ ที่เหลือ (ACCOUNTS/CURRENCIES/BUDGETS/YEAR_GOAL) **เหมือนเดิมหมด ไม่ต้องแก้**:

```js
SUPABASE_URL: "https://xxxxxxxx.supabase.co",     // ← ของโปรเจกต์ใหม่
SUPABASE_ANON_KEY: "sb_publishable_...",          // ← ของโปรเจกต์ใหม่
```

อัปโหลดไฟล์ `config.js` ที่แก้แล้วขึ้น GitHub repo เดิม ทับไฟล์เก่า (ไฟล์อื่นไม่ต้องอัปซ้ำ ยังเป็นชุดเดิม)

## ขั้นที่ 5 · ใส่ยอดยกมา

ตารางว่างเปล่า ก่อนบันทึกรายการปกติ ให้แต่ละคนบันทึก **"ยอดยกมา"** ของช่องทางตัวเอง
(เงินสด/โอน/บัตร) เป็นยอดเริ่มต้นก่อน — `upgrade_v3.sql` กันไว้แล้วว่ายอดยกมาซ้ำช่องทางเดียวกันไม่ได้

---

## ตรวจว่าใช้ได้จริง

1. เปิดเว็บ → **Ctrl+Shift+R** (เคลียร์ cache) → ล็อกอินด้วยอีเมล + รหัสผ่านใหม่ที่ตั้งในขั้นที่ 2
2. บันทึกรายการทดสอบ 1 รายการ **พร้อมแนบสลิป**
3. หน้า **รายการทั้งหมด** → ต้องเห็นรายการนั้น กดปุ่ม **สลิป** เปิดดูได้
4. ลบรายการทดสอบทิ้ง แล้วเริ่มใส่ยอดยกมาจริง

ถ้าขั้นไหนพัง ข้อความ error บนหน้าเว็บจะบอกตรงๆ ว่าใช่ปัญหาคอลัมน์/สิทธิ์/ชื่อบัญชีไม่ตรงหรือเปล่า
(ออกแบบไว้ให้พังแบบบอกสาเหตุ ไม่ใช่เงียบๆ แล้วข้อมูลเพี้ยน)
