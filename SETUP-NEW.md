# ตั้ง Home Ledger ชุดใหม่ให้บ้านอื่น

เอาโค้ดชุดเดียวกันไปตั้งเป็นสมุดบัญชีอีกเล่ม **แยกข้อมูลจากของเดิมสนิท**
คนละฐานข้อมูล คนละบัญชีผู้ใช้ คนละเว็บ — ของเดิมไม่ต้องแก้อะไรเลยสักบรรทัด

| อะไร | ใช้ร่วมกันได้ไหม |
| --- | --- |
| ไฟล์โค้ด (`.dc.html`, `support.js`, `_ds/`, `.sql`) | **ก๊อปไปได้เลย ไม่ต้องแก้** |
| `config.js` | ❌ ต้องเป็นของใหม่ (คนละ URL คนละ key คนละชื่อคน) |
| `schema.sql` บล็อก 5 | ❌ ต้องแก้ให้ตรงกับ `config.js` ชุดใหม่ |
| Supabase project | ❌ ต้องสร้างใหม่ |
| GitHub repo / Pages | ❌ ต้องเป็นที่อยู่ใหม่ |

---

## ขั้นที่ 1 · สร้าง Supabase project ใหม่

**supabase.com → New project** ตั้งชื่ออะไรก็ได้ เลือก region ใกล้ ๆ

เสร็จแล้วไปที่ **Project Settings → API** จดสองค่านี้ไว้:

- **Project URL** เช่น `https://xxxxxxxx.supabase.co`
- **publishable key** (ตัวที่ขึ้นต้น `sb_publishable_...`)

> ⚠️ **ห้ามใช้ `service_role` key เด็ดขาด** ตัวนั้นข้าม RLS ได้ทั้งหมด ถ้าเอาไปใส่ใน `config.js`
> = เปิดฐานข้อมูลทิ้งไว้ให้ทุกคนบนอินเทอร์เน็ต · publishable key เปิดเผยได้ปลอดภัยเพราะอ่านอะไรไม่ได้ถ้าไม่ล็อกอิน

ถ้าโควตาโปรเจกต์ของบัญชีเต็ม ต้องอัปเกรดแพ็กเกจหรือหยุดโปรเจกต์ที่ไม่ใช้ก่อน

## ขั้นที่ 2 · สร้างผู้ใช้ในโปรเจกต์ใหม่

**Authentication → Users → Add user → Create new user** ทำให้ครบทุกคน

- ตั้งรหัสผ่านเอง
- **ติ๊ก Auto Confirm User** ทุกคน (ไม่ติ๊ก = ล็อกอินไม่ได้เพราะรออีเมลยืนยัน)

แล้วไป **Authentication → Sign In / Providers** → **ปิด Allow new users to sign up**
ไม่ปิด = ใครก็สมัครเข้ามาเองได้

จะกี่คนก็ได้ ไม่จำเป็นต้อง 3 คน โค้ดไม่ได้ตรึงจำนวนไว้

## ขั้นที่ 3 · แก้ `config.js`

```js
window.LEDGER_CONFIG = {
  SUPABASE_URL: "https://xxxxxxxx.supabase.co",     // ← ของโปรเจกต์ใหม่
  SUPABASE_ANON_KEY: "sb_publishable_...",          // ← ของโปรเจกต์ใหม่

  APP_NAME: "บ้าน... · Home Ledger",

  CURRENCIES: [                                     // สกุลแรกในลิสต์ = สกุลตั้งต้น
    { code: "THB", sym: "฿", dp: 0, name: "บาท" },
    { code: "KIP", sym: "₭", dp: 0, name: "กีบลาว" },
  ],

  BUDGETS:   [{ cat: "อาหาร", limit: 8000, code: "THB" }],
  YEAR_GOAL: { amount: 200000, code: "THB" },

  ACCOUNTS: [                                       // ← ชื่อกับอีเมลของคนชุดใหม่
    { id: "a1", name: "พ่อ",   role: "บัญชีส่วนตัว",     email: "somchai@example.com" },
    { id: "a2", name: "แม่",   role: "บัญชีส่วนตัว",     email: "malee@example.com" },
    { id: "a3", name: "ส่วนกลาง", role: "บัญชีกลางของบ้าน", email: "home2@example.com" },
  ],
};
```

`cat` ใน `BUDGETS` ต้องสะกดตรงกับชื่อหมวดหมู่ที่ใช้บันทึกจริง ไม่งั้นการ์ดงบจะนับได้ 0

## ขั้นที่ 4 · แก้ `schema.sql` บล็อกที่ 5 ให้ตรงกัน ← **กับดักอันดับ 1**

เปิด `schema.sql` เลื่อนไปท้ายไฟล์ แก้ตาราง `values` ให้ตรงกับ `ACCOUNTS` ข้างบน:

```sql
insert into public.profiles (id, name, role, code)
select u.id, v.name, v.role, v.code
from auth.users u
join (values
  ('somchai@example.com', 'พ่อ',       'admin',  'THB'),
  ('malee@example.com',   'แม่',       'member', 'THB'),
  ('home2@example.com',   'ส่วนกลาง',  'member', 'THB')
) as v(email, name, role, code) on lower(u.email) = v.email
on conflict (id) do update
  set name = excluded.name, role = excluded.role, code = excluded.code;
```

**ชื่อในสองไฟล์นี้ต้องสะกดตรงกันเป๊ะทุกตัวอักษร** เพราะ `txns.who` ผูก FK กับ `profiles.name`
สะกดต่างกันแม้แต่ช่องว่างเดียว = **บันทึกรายการไม่ได้เลย** ขึ้น "ไม่มีบัญชีชื่อนี้ในฐานข้อมูล"

(ข้อดีคือมันพังทันทีและบอกตรง ๆ ไม่ใช่เงียบ ๆ แล้วข้อมูลเพี้ยนทีหลัง)

**ต้องมีคนเป็น `admin` อย่างน้อยหนึ่งคน** ไม่งั้นจะไม่มีใครยกเลิกรายการหรือแก้หมวดหมู่ได้เลย

`code` คือสกุลเงินหลักของคนนั้น ใช้เป็นค่าตั้งต้นตอนบันทึกและตอนเลือกสกุลบนหน้าเดือน

## ขั้นที่ 5 · รัน SQL ในโปรเจกต์ใหม่ **ตามลำดับนี้เท่านั้น**

**SQL Editor → New query → วางทั้งไฟล์ → Run** ทีละไฟล์

| ลำดับ | ไฟล์ | ต้องได้ |
| --- | --- | --- |
| 1 | `schema.sql` | 3 แถว (หรือเท่าจำนวนคน) และมีคนเป็น `admin` |
| 2 | `fix_txn_edits.sql` | ตามที่ไฟล์บอก |
| 3 | `upgrade_v2.sql` | 1 แถว ทุกช่องถูกต้อง |
| 4 | `upgrade_v3.sql` | ✓ ทั้ง 2 แถว |
| 5 | `upgrade_v4.sql` | ✓ ทั้ง 4 แถว |

ถ้าไฟล์ 1 ไม่ขึ้นชื่อครบทุกคน แปลว่าอีเมลใน `schema.sql` ไม่ตรงกับที่สร้างไว้ใน Authentication
กลับไปแก้ให้ตรงแล้วรันไฟล์ 1 ใหม่ (รันซ้ำได้ ไม่พัง)

## ขั้นที่ 6 · เอาขึ้นเว็บ

สร้าง **repo ใหม่บน GitHub** แล้วอัปไฟล์ทั้งหมดขึ้นไป — **รวม `.nojekyll` และโฟลเดอร์ `_ds` ด้วย**
ขาด `.nojekyll` = Jekyll จะข้ามโฟลเดอร์ที่ขึ้นต้นด้วย `_` แล้วหน้าเว็บจะพัง

**Settings → Pages → Deploy from a branch → `main` → `/ (root)`**

จะใช้โฟลเดอร์ย่อยใน repo เดิมก็ได้ แต่คนละ repo แยกกันชัดเจนกว่า

---

## ตรวจว่าใช้ได้จริง

1. เปิดเว็บใหม่ → **Ctrl+Shift+R** → ล็อกอินด้วยอีเมล + รหัสผ่านที่ตั้งไว้
2. บันทึกรายการทดสอบสัก 1 รายการ **พร้อมแนบสลิป**
3. ไปหน้า **รายการทั้งหมด** → ต้องเห็นรายการนั้น และปุ่ม **สลิป** กดเปิดได้
4. เปิดสมุดเล่มเก่าดูด้วย → **ต้องไม่มีรายการทดสอบนี้โผล่** (ยืนยันว่าแยกกันจริง)

ข้อ 4 สำคัญ — ถ้าเห็น แปลว่า `SUPABASE_URL` ใน `config.js` ยังชี้ไปโปรเจกต์เก่าอยู่

---

## สิ่งที่ยังตรึงไว้ในโค้ด (แก้ที่ `config.js` ไม่ได้)

| อะไร | อยู่ที่ไหน | ถ้าอยากเปลี่ยน |
| --- | --- | --- |
| **ช่องทาง** `เงินสด · โอน · บัตร` | `const METHODS` ใน `.dc.html` | การ์ดยอดคงเหลือ 3 ใบผูกกับสามค่านี้ตรง ๆ เปลี่ยนต้องแก้การ์ดด้วย |
| หมวดหมู่ชุดเริ่มต้น | `upgrade_v2.sql` บล็อก 6 | หรือปล่อยไว้แล้วไปแก้เอาทีหลังในหน้า **ตั้งค่า** ได้เลย |
| คำอธิบายบัญชีภาษาลาว | `ACCT_SEED` ใน `.dc.html` | แปลไว้เฉพาะ id `pin` / `ton` / `home` · id อื่นจะแสดงข้อความไทยแทน (แค่คำอธิบาย ไม่กระทบข้อมูล) |

**ค่าที่เก็บลงฐานข้อมูลต้องเป็นภาษาไทยเสมอ** (`method`, `cat`, `kind`) ภาษาลาวเป็นแค่ป้ายที่แสดง
ถ้าจะเพิ่มช่องทางหรือหมวดหมู่เอง อย่าใส่ค่าเป็นภาษาลาวลงไป — ดูหัวข้อนี้ใน `README.md`

## สิ่งที่ **ห้าม** ก๊อปข้ามชุด

- **publishable key ของโปรเจกต์เก่า** — ชี้ผิดฐานข้อมูล ข้อมูลสองบ้านจะปนกัน
- **ไฟล์ในถัง `slips`** — สลิปเป็นเอกสารการเงินของอีกบ้าน ไม่ควรข้ามไป
- **ข้อมูลใน `txns`** — สมุดใหม่ควรเริ่มจากศูนย์ ให้เขาใส่ **ยอดยกมา** ของตัวเองแทน
